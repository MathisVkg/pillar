RENAME TABLE `expense` TO `finpilot_expense`;
RENAME TABLE `external_income` TO `finpilot_external_income`;
RENAME TABLE `income_entry` TO `finpilot_income_entry`;
RENAME TABLE `recurring_expense` TO `finpilot_recurring_expense`;
